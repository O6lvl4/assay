# assay

模型の公称性能が実測に耐えるかを確かめる秤。

`golemide` が使役する側、[`emet`](https://github.com/O6lvl4/emet) が主張の根拠を検査する門、
`assay` が秤です。emet が「文の根拠」を測るのに対し、assay は「スコアの根拠」を測ります。

## なぜ走らせる部分ではなく比較する部分なのか

ベンチマークを走らせる仕組みは、どのベンチも自前で持っています。足りていないのは
**出てきた数字を別の数字の隣に置いてよいかを判定する層**です。

この repo は、ある一日に実際に起きた2つの誤りから設計されています。どちらも
「測っている対象を実際より良く見せる」方向に転びました。自分で踏んだので、
二度と踏めない形にしてあります。

## 規則1: スコアはモデルの性質ではなく、モデルとハーネスの組の性質

Terminal-Bench の保守側は、同一モデルが周囲の足場次第で30〜50点動くと報告しています。
Aider のリーダーボードが行ごとに編集形式と試行数を併記しているのは、それがモデルの
世代交代より大きくスコアを動かすからです。

| Claude Opus 4 (32k thinking), aider polyglot, diff 形式 | 合格率 |
|---|---|
| 1試行 | **37.3%** |
| 2試行（1回目失敗後にテスト出力を渡す） | **72.0%** |

同じモデル、同じベンチ、同じ日で **ほぼ倍**。

だから `Score` は `Harness` を抱えていて、`comparable` は「呼び出し側が“これが試験対象の
軸だ”と宣言していない次元」が食い違う組を拒否します。防いでいる誤りは、
**ハーネスの差を能力の差として読むこと**です。

## 規則2: 構成要素が欠けた合成スコアは、スコアではない

BenchLM の agentic 合成スコアは "normalized by available weights" で、
構成要素はモデルごとに違う形で欠けています。

| 構成要素 | Kimi K3 | Claude Fable 5.1 |
|---|---|---|
| BrowseComp | 91.2 | 掲載なし |
| OSWorld-Verified | 掲載なし | (Fable 5: 85.0) |

したがって公開されている 71.9 と 80.2 は **別の集合の平均**です。これを引き算すると、
あるいはベンダー公表値からの再構成を引き算すると、何も記述していない数字が出ます。

その引き算は実際に行われ、存在しない13点の「ハーネス損失」を生み、
その差を追って一日が費われました。

so `coverage` は **実際に覆えた重みと、見つからなかった構成要素を必ず返し**、
`coverage_incomparable` は found-set が違う2モデルの比較を名指しで拒否します。

## 今の中身

`src/score.almd` — 上の2規則を型と関数にしたもの。
`src/published.almd` — 一次リーダーボードから読んだ数字と、その不在。
`src/available.almd` — 実際に呼べるモデル（後述）。計14テスト。
`bench/tb2.sh` と `bench/tb2-score.py` — Terminal-Bench 2.0 の走行と集計。

比較層を先に固めたのは意図的で、**信用できない比較器に流し込む測定値を増やしても、
間違いの量が増えるだけ**だからです。走らせる部分は後から足しました。

## 的として検討したベンチと、その評価

コーディング特化を狙う場合。すべて2026年9月時点の一次確認。

| ベンチ | 現行Claude | オープンウェイト最良 | 的として |
|---|---|---|---|
| SWE-bench **Verified** | Opus 5 **96%** | 掲載なし | **不適**（飽和） |
| SWE-bench **Pro** | Fable 5.1 **81.2%** | Qwen3.8 Max 67.7% / GLM-5.2 62.1% | **最有力**（未飽和・13.5〜19点差・GLM-5.3 未測定） |
| Aider Polyglot | Opus **4** 72.0%（2025-05のモデル） | Kimi K2 60.0% | 現行Claudeが載っていない |
| BenchLM agentic 合成 | Fable 5.1 80.2 | Kimi K3 71.9 | **不適** — 理由は2つあり、決定的なのは後者（下記） |

合成が的として不適な理由は二段ある。どちらも正しいが、強さが違う。

1. **重みの60%がコーディングではない。** 構成は Terminal-Bench 2.0 が30%、
   BrowseComp 25%、OSWorld-Verified 25%、OSWorld 2.0 10%。後ろの3つ（計60%）は
   ウェブ検索とGUI操作で、コーディング能力ではない。コーディングエージェントを
   どれだけ良くしても、この数字は3割しか動かない。
2. **合成は分解できない。** Fable 5.1 は 80.2 を保持しているのに、自分の構成要素の
   リーダーボード（Terminal-Bench 2.0・BrowseComp・OSWorld-Verified）に**一つも
   載っていない**。載っているのは Fable 5 / Mythos 5 / Sonnet 5 という別の行。
   だから 80.2 はどの公開構成要素値からも再現できず、どちらの向きにも計算できない。
   **「あと何点足りないか」を求める方法が存在しない。**

1 は「効率が悪い的」と言っているだけだが、2 は「的として使えない」と言っている。
この repo の創設時の誤り（存在しない13点）は、まさに 2 を知らずに 1 の上で
引き算をしたものだった。

最後の行は覚えておく価値があります。agentic 合成の構成は
Terminal-Bench 2.0 が30%、BrowseComp 25%、OSWorld-Verified 25%、OSWorld 2.0 10%。
**約60%がコーディングと無関係**なので、コーディングエージェントのハーネスを直しても
あの数字は動きません。これは算術ミスとは別に、一日が空振りした構造的な理由です。

## まだ無いもの

- 走行アダプタ（SWE-bench Pro から始める）
- 公開リーダーボードの取り込み（数字とURLと、その行のハーネス構成を一緒に）
- 報告。`tenths` があるのは、`-0.7000000000000028` を人に見せないため

## 出典

- https://aider.chat/docs/leaderboards/ — 試行別合格率と編集形式
- https://benchlm.ai/agentic — 合成スコアの重みと "normalized by available weights"
- https://benchlm.ai/benchmarks/browsecomp ・ https://benchlm.ai/benchmarks/osworld-verified — 掲載モデルの非対称
- https://llm-stats.com/benchmarks/swe-bench-verified ・ SWE-bench Pro リーダーボード

## 測って出た、構造的な壁

Terminal-Bench 2.0（合成の30%、89タスク）に golemide を繋いで走らせた結果。
アダプタは通り、2試行が例外なく完走し、両方ゼロ点。理由はログにそのまま出ている:

    no verify command found under /app; leaving this trial unattempted

発見ロジックの不具合ではない。タスクコンテナには `gates.txt` と `sim.c` があり、
**テストが一つも無い**。harbor はエージェント終了後にテストを注入して採点する
（oracle が 1.0 を取るのはその経路）。**Terminal-Bench は成功信号を意図的に渡さない。**

golemide のインターフェースは全体が `--verify CMD` —「終了ステータスが成否を定義する
コマンド」。このベンチにはエージェントが持てるそんなコマンドが存在しない。だから
golemide は設計上ここで競えず、アダプタをいくら作り込んでも変わらない。
**このベンチが測っているのは、golemide が呼び出し側に委ねている唯一のもの** —
教えられずに「終わった」と判断すること。

これは [emet](https://github.com/O6lvl4/emet) の設計文書が入口ゲートとして名指している
差と同じもの:「入口は、要求を受け入れ基準に変えられるかを問う」。測る前に、
欠けている部品として書かれていた。

正直な境界線はここ。テスト駆動のループは、より安く・より慎重に・より的を絞って
動かせる（すべて実測済み・すべて本物）。それでも、**何が合格かを教えることを拒む
ベンチには入れない。** それを閉じるには、散文から受け入れ基準を作り、作れないときは
棄権する部品が要る。これはこのプログラムとは別のプログラムである。

### 経路の実測値（記録用）

| | |
|---|---|
| oracle 1タスク | reward 1.000 / 1分45秒 / $0 |
| golemide 2タスク | 例外0 / reward 0.0 / 3分47秒 / 未実施のため$0 |
| 到達不能の理由 | 受け入れ基準がコンテナ内に存在しない |

到達までに潰した非互換（すべてモデル呼び出し前に検出、合計$0）:
Python 3.14 は terminal-bench が PEP 649 非対応 → 3.12 /
`terminal-bench` パッケージに 2.0 は無い → `harbor` /
aarch64 バイナリはタスクが x86_64 で動かず /
x86_64 は GLIBC_2.39 リンクでタスクは 2.36 /
bookworm での almide ソースビルドはメモリ不足 /
`CARGO_BUILD_TARGET=musl` は `almide build` に無視される →
**ホスト実行＋`docker exec` 橋渡しに設計変更**

### 導出を試して、床が足りないことも測った

「成功信号が無いなら導出すればよい」を実装して走らせた。harbor は独立に採点するので
導出信号で点を偽装することは構造的に不可能 — 弱い信号は誘導に失敗することしかできない。
なので「タスクが同梱するコードがコンパイルでき実行できる」という、主張としては最弱だが
主張ではあるものを導出した。

**2タスクとも発火しなかった。** Terminal-Bench のタスクは演習の形をしていない:

    circuit-fibsqrt        /app = gates.txt, sim.c
    make-mips-interpreter  /app = doom.wad, doomgeneric, doomgeneric_mips

後者は「Doom を走らせられる MIPS インタプリタを書け」。機械的なビルド確認は何の意味も
持たず、トップレベルの `.c` に `cc` を当てるという発想自体が適用外。**タスクが形を
共有しないことを要点にしているベンチで、形ごとに信号を導出するのは負ける勝負。**

だから導出関数は単純なタスク向けの床として残し、意図的に拡張していない。
**これが露出させた差は、ヒューリスティックの不足ではない。**

## 結論

このセッションの出発点は「オープンウェイトで Claude 級のコーディングエージェントが
作れるか」で、2番目の訂正が「汎用性だよ？俺が言ってるのは」だった。それを
「外部の成功信号なしに動けるか」と読み直した。

その読み直しが、スタックの最下層で実測された。合成スコアの最大構成要素が測っているのは
まさにそれで、プロンプト量でも編集形式でも算術でもなかった。

| 動かせたもの | 実測 |
|---|---|
| 1試行あたりコスト | −38〜41.5%（3回再現） |
| 実害バグ | 7件修正 |
| プロンプト量と合格率 | **非連動**（2コーパス・p=1.000×3） |

| 動かせなかったもの | 理由 |
|---|---|
| 合格率 | プロンプト削減では動かない（実測） |
| 合成スコア | 分解不能（80.2 の保持者が構成要素に不在） |
| Terminal-Bench 2.0 | **受け入れ基準がコンテナ内に存在しない** |

テスト駆動のループは、より安く・より慎重に・より的を絞って動かせる。それでも
何が合格かを教えることを拒むベンチには入れない。閉じるには emet の入口ゲート —
散文から受け入れ基準を作り、作れないときは棄権する部品 — が要る。設計済み・未実装。

### 入口ゲートを最小実装して、3段の深さで壁を測った

「成功信号が無いならモデルに述べさせればよい」を実装した。emet の第1原則に従い、
**判定せず測る**形にした: モデルが述べた基準を**編集前のコンテナで実際に走らせ、
落ちることを確認**する。編集前に通る基準は編集を導けないので棄権する。

3段の深さで壁が出た。

**第1段。** タスクコンテナに受け入れ基準が存在しない。harbor がエージェント終了後に
テストを注入する。so 検証コマンドを前提とするループは一歩も動かない。

**第2段。** 機械的な導出は一般化しない。`circuit-fibsqrt` は `/app` に `gates.txt` と
`sim.c`、`make-mips-interpreter` は `doom.wad` と `doomgeneric/`。後者に「トップレベルの
.c を cc する」は適用外。**タスクが形を共有しないことを要点にしているベンチで、形ごとに
信号を導出するのは負ける勝負。**

**第3段、これが一番重要。** モデルに検証コマンドを求めると、**解答を検査に偽装して
返してくる**。実測した返答:

    sh -c 'set -e
    cat > mips.c <<EOF
    #include <stdio.h>
    ...

これは検査ではなく実装。弁別テストは「常に通る基準」を落とせるが、**「自分の答えを
内包した基準」は落とせない。** 単一のモデル呼び出しには、基準と解答を区別する手段が無い。

だから emet の入口ゲートは、単発の呼び出しではなく **k個の解釈をサンプリングして
食い違ったら棄権する**設計になっている。測っているのは答えの正しさではなく
**要求の決定性**で、それが単発呼び出しにできないことの正体。

### 途中で見つけた、計器側の誤り

第3段の実装で3つバグを出し、全部「測定対象の性質」に見える形で現れた。

| バグ | どう見えたか |
|---|---|
| `result.response` を読む（実際は `choices[0].message.content`） | 「モデルが基準を作れなかった」 |
| `max_tokens: 300`（推論モデルが全部推論に使う） | 同上・`finish_reason=length` で content 空 |
| 資格情報チェックが無言で棄権 | ログに何も残らず原因不明 |

`reasoning_effort: low` + `max_tokens: 8192` で `finish=stop` と実内容が返る。
**棄権は必ず理由を残す**ようにした — 無言の棄権は原理に基づく棄権と見分けがつかず、
今日それで1回実行を無駄にした。これが assay を作った理由そのもの。

### 入口ゲートが実際に棄権した。これは設計どおりの帰結

k=3 サンプリングの多数決ゲートを実装して `make-mips-interpreter` に当てた結果:

    a criteria sample returned no content (finish_reason=length)
    a criteria sample returned no content (finish_reason=length)
    only 1 criteria sample(s) came back; not enough to measure agreement, abstaining

3つのうち2つが予算内で基準を述べきれず、多数決が成立せず棄権した。

**これはバグではない。** 「Doom を走らせられる MIPS インタプリタを書け」は一行の検査に
還元できない要求で、ゲートはそれを検出して棄権した。棄権はゲートの失敗ではなく目的。

そして棄権すれば golemide は何も試行せず0点になる。**正直に0点を取ることが設計どおりの
帰結**で、ここに残っているのは実装の不足ではなく**アーキテクチャの違い**である:

| | 前提 |
|---|---|
| golemide | 検証可能な成功信号がある。無ければ棄権する |
| Terminal-Bench 2.0 が要求するもの | 検証可能な信号が無い状態で行動し、自分で完了を判断する |

Claude Code を超えるには後者が必要で、それは「テスト駆動ループを速く安くする」の延長では
到達しない。**不確実性の下で行動するエージェント**という別の設計である。

### 同じ型のバグを3回出した

入口ゲートの実装中に出したバグは、すべて「モデルが基準を作れなかった」という
**正しそうな結論に化けた**。

| バグ | 化けた結論 |
|---|---|
| `result.response` を読む（実際は `choices[0].message.content`） | モデルが基準を作れなかった |
| `max_tokens: 300`（推論モデルが全部消費） | 同上 |
| `_sample_criteria` の空応答が無言 | 同上・ログに何も残らない |

共通構造は **「測定側の欠陥が、測定対象の性質として現れる」**。今日これで6件目
(55%の集計バグ、0.3%の測り方、13点の算術、無言の資格情報チェック、上記3件)。
assay の存在理由が、そのまま作者に返ってきた。**全ての棄権に理由を残す**よう直した。

### 信号なしモードは動いたが、遅すぎた。そしてコスト記録を失った

第4段(信号なし最善努力)を `make-mips-interpreter` に当てた結果:

    no criteria could be discovered, derived or stated for /app;
    running 2 best-effort passes with no success signal instead of not attempting
    ...
    AgentTimeoutError: Agent execution timed out after 1800.0 seconds

**golemide は初めて実際に行動した** — 30分間。三段のゲートが棄権した上で第4段が発動し、
「棄権して0点」から「行動して時間切れ」に変わった。ただし採点対象の成果には至らず。

**そしてその30分のコスト記録を失った。** アダプタが `communicate()` でパイプを使い、
プロセス終了までメモリに溜める設計だったので、harbor に殺されると**ログ全体が消えた** —
コスト行を含めて。30分の支出が報告不能になった。これは後から再構成できない唯一の種類の
欠損である。

直した: golemide の出力をファイルへ直接書かせ(随時ディスクに残る)、コスト抽出を
`finally` に移した。**殺された実行でもコストが記録される。** 実測で確認済み —
実行中のログがディスクに存在し、kill 後も残り、`$0.0123` が抽出できる。

今日7件目の「計器側の誤り」で、しかも**課金の記録を失わせる**種類だった。

速度については、置換編集形式(golemide fda48a0、10KBのファイルで実時間-92%)を
含むビルドで30分かかっている。**この規模では置換だけでは足りない。**

---

# 第二部: 実際にスコアを測った記録（2026-09-13 夜）

ここから先は、上の「壁」を認めた上で実際に89タスクのベンチを走らせた記録。
**測定できるようになり、測り、そして測定自体が不安定であることを知った**までを残す。

## ベンチマークを「何を測っているか」で分類する

リーダーボードは数字を並べるが、**どの形のエージェントを報酬するか**を書かない。
今夜の実測で、そこが決定的だと分かった。

### 1. 既存コードを直す（テストは隠される）

| ベンチ | 現行Claude | オープン最良 | 的として |
|---|---|---|---|
| SWE-bench Verified | Opus 5 **96%** | 掲載なし | 飽和。差がつかない |
| SWE-bench Pro | Fable 5.1 **81.2** | Qwen3.8 Max 67.7 / GLM-5.2 62.1 | 未飽和・GLM-5.3 未測定 |

エージェントの形: リポジトリと課題を渡され、パッチを出す。テストは見えない。

### 2. 見えるテストを通す（フィードバックループあり）

| ベンチ | 現行Claude | オープン最良 |
|---|---|---|
| Aider Polyglot（225問・6言語） | **載っていない**（最良は Opus 4、2025-05） | Kimi K2 60.0 |

エージェントの形: **golemide そのもの**。`--verify` の終了ステータスが成功を定義する
検証駆動ループ。構造的にここが一番強いはずの場所で、そして**現行Claudeが載っていない**
ので「Claude を超えた」とは言えない場所でもある。

### 3. ターミナル（成功信号なし）

| モデル | Terminal-Bench 2.0 |
|---|---|
| Kimi K3（オープン・**呼べない**） | 88.3 |
| Claude Mythos 5 | 88.0 |
| Claude Fable 5 | 84.3 |
| **GLM-5.2**（オープン・呼べる） | **81.0** |
| Claude Sonnet 5 | 80.4 |
| Claude Opus 4.8 | 74.6 |
| GLM-5.1 | 63.5 |
| **GLM-5.3**（呼べる） | **世界に存在しない** |
| **golemide + GLM-5.3（今夜の実測）** | **1/9 = 11.1%**（10タスク予備走行） |

エージェントの形: コマンドを走らせ、**信号なしで自分で完了を判断**する。

### 4. コーディングではない部分（合成の60%）

| ベンチ | 内容 | 首位 |
|---|---|---|
| BrowseComp | ウェブ検索 | Kimi K3 91.2 > Opus 5 90.8 |
| OSWorld-Verified | GUI 操作 | Qwen3.8 Max 86.1 > Fable 5 85.0 |
| OSWorld 2.0 | GUI 操作 | 合成の10% |

## 目標と宣言が違うベンチを指している

このプロジェクトには二つの言明がある。**同じ的を指していない。**

| 言明 | 対応するベンチ |
|---|---|
| 「汎用エージェントスコアで claude code を超える」 | BenchLM agentic 合成。**60%がコーディングではない** |
| 「最高のコーディングエージェントをオープンウェイトで」 | SWE-bench Pro / Aider Polyglot / Terminal-Bench 2.0 |

汎用スコアで超えるには、ブラウザ操作とGUI操作のエージェントを別に作る話になる。
コーディングエージェントの延長線上には無い。**この二つを混同すると、
コーディングを改善して汎用スコアが動かないことに驚く**ことになる。

推奨は **Terminal-Bench 2.0 に的を絞る**こと。理由は三つ。

1. 合成の構成要素のうち、コーディング能力を測る唯一のもの（30%）
2. **GLM-5.3 のスコアが存在しない。** 作れば我々が最初
3. `claude-code` が harbor の組み込みエージェントなので、**同じ89タスク・同じ
   ハーネスで直接対戦できる。** 足場非開示の数字と比べる必要が消える

## 到達可能性はスコアと別の事実（`src/available.almd`）

リーダーボードは「どれが最強か」に答えるが「このプロジェクトが動かせるのはどれか」
には答えず、二つの答えは**最上位で食い違う**。

Cloudflare Workers AI カタログ（Text Generation 31件、2026-09-13 API 確認）:

| | |
|---|---|
| 呼べる | glm-5.3, glm-5.3-flash, glm-5.2, glm-4.7-flash, kimi-k2.7-code, kimi-k2.6, qwen3.8-27b, qwen3-30b-a3b-fp8, qwq-32b, qwen2.5-coder-32b, deepseek-v4-pro, deepseek-v4-flash, llama-4-scout |
| **呼べない** | **Kimi K3**（Terminal-Bench 2.0 首位 88.3）、qwen3.8-max（OSWorld-Verified 首位 86.1） |

`best_reachable("terminal-bench", "2.0")` = **GLM-5.2 の 81.0**。これは
Claude Sonnet 5 (80.4) と Opus 4.8 (74.6) を**既に上回り**、Fable 5 (84.3) には届かない。
両方が同時に見えている必要がある。

「Kimi K3 でないとダメか」への答え: **違う。だがスコア表から答えると「そう」になる。**
スコア表は制約ではない。

## Terminal-Bench 2.0 の構造（89タスク、実測）

| | |
|---|---|
| 難易度 | easy 4 / medium 55 / hard 30 |
| `[agent] timeout_sec` | 600〜12000秒。全タスクが壁まで走ると **41.3時間** |
| 専門家所要時間 | 中央値60分、最大2400分（40時間） |
| **ソース非同梱** | **26/89** — package は Dockerfile と指示のみ |

最初の測定対象に選んだ `circuit-fibsqrt` は `difficulty="hard"`、
`expert_time_estimate_min=960`（**専門家16時間**）。
**エージェントの能力を判断する最初の一手として最悪の選択だった。**

## 予備走行を3回。スコアは出たが、安定しない

10タスク（easy 2 + medium 8、タイムアウト900〜1800秒）。

| 走行 | 並列 | 結果 | 備考 |
|---|---|---|---|
| 1回目 | 4 | **1/9 = 11.1%**、$0.2891以上 | polyglot-c-py が reward 1.0 |
| 2回目 | 4 | **0/4 = 0.0%**、RuntimeError 6件 | 1回目に解けた polyglot-c-py も33秒で落ちた |
| 3回目 | 2 | 3タスクで中断 | log-summary-date-ranges が初めて完走 |

**まだ「GLM-5.3 が何点取るか」を安定して測れていない。** ハーネスの脆さが
モデルの能力を覆い隠していて、スコアの上下が修正の効果か運かを分離できない。
次に必要なのは改善ではなく、**同じ結果が2回出る状態**である。

## 予備走行が見つけた欠陥（すべて実測、すべて修正済み）

実測は、机上では見つからなかったものを即座に出した。

### 1. 空のルートを拒否していた（26/89 に影響）

即死した3タスク（filter-js-from-html, count-dataset-tokens,
openssl-selfsigned-cert）は45秒以内・**$0.00**・reward 0 で終わり、
**モデルを一度も呼んでいなかった**。

    project: unknown (no marker), 0 files
    no source files found ... nothing here can be read, so nothing can be fixed.

判定基準の不足ではない。この3つは「`/app/filter.py` を作れ」という課題で、
**直す対象が存在しない**。golemide は修理道具なので、読むものが無いと拒否する。

`--create`（`allow_create`）は既にあって存在しないファイルへの書き込みを許していた。
`main.almd` の空ルート拒否が、そのフラグを読む前に発火していただけ。
実機確認: 空ディレクトリ + `--create` で1回・$0.000266・4秒で通った。

### 2. 探査の失敗が trial を落としていた

`RuntimeError: Command timed out after 20 seconds` が trial を終わらせていた。
**reward 0 ではなく reward 無し** — ゼロですらない不在。正しい結末は、その探査
ひとつが「否」と答えて次の段が試されることだった。

アダプタには `exec` が6箇所あって、例外を捕まえていたのは1箇所だけ。
`_probe()` を入れて4箇所を通し、走れなかった探査は「この段は棄権」として扱う。
**走って非ゼロで終わったこととは区別される。**

### 3. マーカー検出が8往復していた（これが2回目の走行を殺した）

`_discover_verify` は候補マーカー8個に対して `exec` を8回投げていた。中身は
`test -e` だが、毎回が新しい `docker compose exec` で、その起動コストを8回払い、
それぞれに20秒の上限がかかる。並列4でこの上限は寛大ではない。
**高かったのは `test` ではなく8回の往復。**

1往復に畳み、上限を90秒にした。前の走行が通ったのは運で、今回落ちたのも運。

### 4. `-i` はorg接頭辞付きの完全名で照合する

`-i fix-git` は何にも一致せず、harbor は trial 開始前に例外を投げる。
**最初の起動は1秒で全滅した。** 89タスクで踏んでいたら発見に5時間かかっていた類で、
予備走行を先に置いた理由がこれ。接頭辞の補完を `tb2.sh` 側に入れた。

### 5. 導出した判定基準に識別テストが無かった

識別テストが、たまたま検討中だった段（stated）にだけ付いていた。
**原則が特例になる形。** `circuit-fibsqrt` の導出判定
`cc -O1 -o /tmp/_assay_build sim.c && /tmp/_assay_build` は手付かずで既に通る
（`sim.c` は普通にコンパイルできて走る。問われているのは計算内容）。
golemide は正しく「既に通る」と拒否し、trial は何も導けない基準の上でゼロを取った。

## `reasoning_effort` は効く。予算を上げるのは弱いレバー

glm-5.3、同一プロンプト、同一4096トークン（2026-09-13 実測）:

| effort | finish | answer | reasoning | 消費 |
|---|---|---|---|---|
| `low` | **stop** | **2,620字** | 3,269字 | 2,146 |
| `high` | length | **0字** | 12,346字 | 4,096（全消費） |

**推論は与えられた分だけ膨らむ。** だから推論が予算を食い尽くした時に予算を3倍に
するのは弱いレバーで、単体では推論に3倍の余地を渡すだけ。同じ問題を `low` は
2,146トークンで完答している。

golemide の `ask_json` は、まさにその弱いレバーだけを引いていた。408タイムアウトの
経路は effort を下げるのに、推論由来の切り詰めは下げない — **二つのレバーが
交差していた。** err 経路で effort も一段下げ、`settle()` の助言も原因別にした
（推論由来なら "spend less on reasoning"、推論が空なら "raise max_completion_tokens"）。

## 今夜した自己訂正

| 言ったこと | 実際 |
|---|---|
| 「モデルではこのタスクは解けない」 | 単発プロンプトの結果からの過度な一般化。エージェントループではない |
| 「出力が大きすぎて書けない」 | 例の gates.txt は33行。`MAX_SIGNALS 32000` は信号数上限で、32,000ステップ回る**順序回路**なので小さく書ける。**設計難度の問題** |
| 「harbor は1800秒で殺す」 | タスクごとの `[agent] timeout_sec`（600〜12000秒）。単一の締切は存在しない |
| 「`-n` は試行数」 | `--n-concurrent`（並列数）。試行数は `-k` |
| 「レバーが交差しているのがこの失敗の原因」 | attempt 2 は遅延ガードで既に `low` に落ちていた。下げ代が無かったので、この失敗の原因ではない |
| 「作業ツリーが汚れているから push を待つ」 | push はコミットだけを送る。過剰だった |

## 費用の記録について

`bench/tb2-score.py` は費用を必ず**下限**として報告する。golemide は summary 行に
支出を出すので、その行の前に殺された trial の支出は見えない。

今夜、中断した `circuit-fibsqrt` の走行（`CancelledError`、1869秒）は
**reward も cost も記録されていない**。前回の同一走行が $0.002196 だったので
同程度と推定されるが、**推定である**。正確な額は Cloudflare のダッシュボードが持つ。

そして job 直下の要約ではなく各 trial の `result.json` を読むのは、両者が肝心な
場合に食い違うから: 中断・異常終了した trial は reward も cost も記録せず、
要約はそれをゼロとして平均に入れてしまう。**タスクに失敗して得たゼロと、
終わらなかった trial のゼロは別の測定**で、exception 列がそれを分ける。

## 結論（第二部）

| 動いたもの | 実測 |
|---|---|
| 測定経路 | harbor 0.23.0 / 89タスク取得 / oracle reward 1.000 |
| 初めて出たスコア | **1/9 = 11.1%** |
| 修正した欠陥 | 10件（うち5件は測定が見つけた） |

| 動いていないもの | 理由 |
|---|---|
| スコア | 11.1% 対 Claude 74〜88。**遠く及ばない** |
| 測定の再現性 | 3回走って 11.1% / 測定不成立 / 中断 |

そして第一部の結論は、測ってもなお正しい。

| | 前提 |
|---|---|
| golemide | 検証可能な成功信号がある。無ければ棄権する |
| Terminal-Bench 2.0 | 信号が無い状態で行動し、自分で完了を判断する |

さらに測って分かった二点が、この差を広げる。

- **26/89 は「作る」課題で、「直す」課題ではない。** golemide の前提は修理
- **課題はコマンドを走らせて反復する必要がある。** サーバを起動し、生成器を書いて
  走らせ、状態を見る。golemide はファイルを書いて verify を1回走らせる形で、
  `--explore` も開始前の読み取りに限られる

加えて、ホストから `docker exec` でブリッジしている構造自体が症状である。
**エージェントは作業がある場所に居るべきで**、今夜のアダプタ不具合の大半
（8往復・20秒上限・trial 消滅）はこのブリッジ由来だった。

Terminal-Bench が要求しているのは名前のとおり**ターミナルエージェント**である。
コマンドを走らせ、出力を見て、判断し、反復し、自分で完了を決める。
これは golemide とは別の形であり、継ぎ足しでは届かない。
